streamlit
transformers>=4.30.0 # Ensure a recent version for PEFT and bitsandbytes compatibility
torch>=2.0.0 # Ensure a recent version
wikipedia-api
accelerate>=0.21.0 # For bitsandbytes and PEFT utilities
peft>=0.7.0 # For LoRA
bitsandbytes>=0.41.0 # For 4-bit quantization
# sentencepiece # Often a dependency for DeBERTa tokenizers, transformers might pull it in
